
public class StringMethods {
    
    public static void main(String[] args) {
        
        String name = "Sukku";

        // boolean result = name.equals("sukku");
        // boolean result = name.equalsIgnoreCase("sukku");
        // char result = name.charAt(4);
        // int result = name.indexOf("u");
        // boolean result = name.isEmpty();
        // String result = name.toUpperCase();
        // String result = name.toLowerCase();
        // String result = name.trim();
        String result = name.replace("u","u");

        System.out.println(result);
        
    }
}

// string = a reference data type that can store one or more characters 
// equals => it checks equal or not
// equalsignorecase => it ignore case sensitive letters
