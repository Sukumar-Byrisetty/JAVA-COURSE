package VariableScope;

public class VariableScope {

    public static void main(String[] args) {

        DiceRoller diceRoller = new DiceRoller();

    }
}

// local = declared inside of a method visible only to that method
// global = declared outside of a method, but within a class visible to all
// parts of a class.
